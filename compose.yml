services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=securebank_db'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=securebank_user'
      - 'MYSQL_PASSWORD=secret'
    ports:
      - '3306:3306'
